# zmm-flow · 评测样本

每个样本一对文件：`sample-NN-<描述>.md`（给执行者）和 `sample-NN-答案.md`（只给评分者）。
执行者是一个**没看过答案的新 context**，拿 SKILL.md + 样本跑出报告，报告存到 `results/sample-NN-v<版本>.md`。

## 记录

| 样本 | 版本 | 命中 / 满分 | 误报 | 结论 |
|---|---|---|---|---|
| 01 白板稿 | v0.1.0 | 4 / 4 | 0 | 基线 |
| 01 白板稿 | v0.2.0 | 4 / 4 | 0 | 通过（≥ 基线）。另多报两处「等的没来」：原因三只给了一个字没给样子 · 结尾没接开头「看自己中了哪一个」的钩，均成立 |

改 SKILL.md 的行为之后，先跑一遍样本再提交；命中低于上一版就不许发。
